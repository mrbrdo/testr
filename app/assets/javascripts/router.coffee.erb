class WebApp.Router extends Backbone.Router
  routes: <%= BackboneRoutes.instance.to_routes %>

  initialize: ->

  dashboard: ->
    @check_token()
    WebApp.show_view new WebApp.Views.Dashboard()

  check_token: ->
    unless H.access_token()?
      H.go_to_login()
